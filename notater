ting som skjer:

Jeg setter i gang main(String[] args).

Main.main(args) kaller launch(args). Launch er tilgjengelig i Main fordi Main extends
Application.

[noe skjer, vet ikke hva]

Grafikkvinduet åpnes.


setup lager et nytt spill, starter spillet og kaller doTurn(). spillet venter på input.

hvis DEBUG-time er true så blir det en del prints-setninger







launch()
Launch a standalone application. This method is typically called
     * from the main method(). It must not be called more than once or an
     * exception will be thrown.
     * This is equivalent to launch(TheClass.class, args) where TheClass is the
     * immediately enclosing class of the method that called launch. It must
     * be a subclass of Application or a RuntimeException will be thrown.

